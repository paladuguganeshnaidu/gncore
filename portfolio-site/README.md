# Portfolio site demo

This folder contains a lightweight static portfolio website that demonstrates how the GNCore package can support website content and workflow-driven structure.

## Run locally

From the repository root, start a simple static server:

```powershell
python -m http.server 8000 --directory portfolio-site
```

Then open http://localhost:8000/ in your browser.
