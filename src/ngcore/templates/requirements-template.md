# Requirements — <project name>

## Purpose

One sentence: what does this site/app do and for whom.

## Audience

Who uses it, technical level, devices/browsers to support.

## Features

| Feature | Priority (must/should/could) | Notes |
| --- | --- | --- |

## Explicit non-goals

What this project deliberately does NOT do (prevents scope creep later).

## Non-functional requirements

- Performance budget: e.g. LCP < 2.5s on 4G, JS bundle < 200KB gzipped
- Accessibility target: WCAG 2.2 AA
- Security posture: e.g. handles PII? payments? auth?
- Coverage floor: e.g. 80% on critical paths

## Internationalization (optional)

Multiple languages/locales needed? If not, state "single-language, <language>" explicitly rather than leaving this blank — that's still a decision, not an omission.

## Constraints

Budget, timeline, existing infra, must-use platform, etc.

## Approval

- [ ] User has explicitly approved this document as written.
